import styled from '@emotion/styled';

const Form = styled.div`
  display: flex;
  justify-content: space-between;
  flex-direction: row;
  width: 100%;
  margin-bottom: 1rem;
`;

export default Form;